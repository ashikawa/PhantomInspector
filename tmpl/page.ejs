## [<%- setting.name %>](<%- setting.url %>)

Title
: <%- title %>

URL
: <%- setting.url %>

<% if (setting.userAgent) { %>UserAgent
: <%- setting.userAgent %>
<% } %>
<% if (setting.capture) { %>Capture
: ![capture](<%- setting.capture %>)
<% } %>

### Metas

|name|property|content|
|---|---|---|<% for(var i = 0; i < metas.length; i++) { %>
|<%- metas[i].name %> |<%- metas[i].property %> |<%- metas[i].content.replace(/\|/g, '\\|') %> |<% } %>
<%
    for(var i = 0; i < records.length; i++) {
        response = records[i];
        parsed   = url.parse(response.url, true, true);
%>
### Analytics Becon (\# <%- i %>)

|param|value|
|---|---|<% for (var key in parsed.query) { %>
|<%- analytics.convert(key) %>|<%- parsed.query[key].replace(/\|/g, '\\|') %>|<% } %>
<% } %>